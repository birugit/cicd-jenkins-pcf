Login and Push a springboot app to PCF
Create a manifest.yml file
   which is used to sepcify the app target and memory required.

$cf login -a https://api.run.pivotal.io
$cf push

//delete existing app
$ cf delete -r APP-NAME
